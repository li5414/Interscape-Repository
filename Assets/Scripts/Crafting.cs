using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Crafting : MonoBehaviour
{
	// can use grass but using a table increases likelyhood of better quality
	// drag and drop things together (huge invisible grid)
	// some crafting things require certain tools (drag and drop item onto/into tool to get it)
	// tools can be left on the workbench or reside in inventory (obviously some tools will be too heavy to carry around)



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
